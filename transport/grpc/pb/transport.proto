syntax = "proto3";

option go_package = "yeager/transport/grpc/pb";

package pb;

service Transport {
    rpc Tunnel (stream Data) returns (stream Data) {
    }
}

message Data {
    bytes data = 1;
}
