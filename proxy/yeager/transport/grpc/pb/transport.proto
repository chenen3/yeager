syntax = "proto3";

option go_package = "github.com/chenen3/yeager/proxy/yeager/transport/grpc/pb";

package pb;

service Tunnel {
    rpc Stream (stream Data) returns (stream Data) {
    }
}

message Data {
    bytes data = 1;
}
